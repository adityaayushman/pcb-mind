fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
asyncpg==0.29.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
supabase==2.7.4
httpx==0.27.2
pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# CPU-only PyTorch build: the default PyPI wheel pulls in the full CUDA
# toolkit (nvidia-cublas, nvidia-cudnn, triton, ...), which is dead weight
# on a CPU-only host and dramatically inflates both image size and runtime
# memory — enough to OOM a 512MB instance just loading the model.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

ultralytics==8.2.103
reportlab==4.2.2
python-dotenv==1.0.1
